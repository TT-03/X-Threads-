name: Schedule Runner

on:
  schedule:
    - cron: "* * * * *"  # –ˆ•ª
  workflow_dispatch: {}   # è“®Às‚àc‚·

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Call schedule runner endpoint
        run: |
          curl -fsS \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/schedule/run"
          echo ""
